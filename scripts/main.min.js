'use strict';

var twitterize = function twitterize(link) {

  var tweet = document.getElementById('tweetText').innerHTML;

  document.getElementById('tweetText').innerHTML = anchorme(tweet);
};

twitterize();